# M3 Evidence — Solver Adapter Contract

**Status:** **S2 — Implemented Candidate / Owner Verification Pending**

**Implementation commit:** `66c5971d0f52262b4e6c9f968ecff0fe55abf9a8`
**CI:** pending for the implementation commit

This packet records the M3 contract implementation and local reproducible
evidence. It does not claim S3 until the implementation commit's CI and owner
verification are complete.

## Scope

`packages/sci` now owns the Scientific Reality Core adapter boundary while
depending only on `@chemrealm/schema`. The schema package remains the single
source of truth for DTO/result shapes and their JSON Schema artifact. M3 does
not implement chemistry, PHREEQC, solver disagreement averaging, or ADR-0008's
Tier A/B/C product workflows.

## Acceptance matrix

| Criterion | Result | Evidence |
|---|---|---|
| AC-R6 — exact solver identity lookup | PASS | `packages/sci/src/registry.test.ts` — `id + version` mismatch returns `unavailable`; no newer-version fallback |
| AC-S4 — refusal is a normal tagged result | PASS | `packages/sci/src/adapter.test.ts` — out-of-range temperature returns `MODEL_OUT_OF_DOMAIN`, includes `nearestSupported`, and does not invoke the solver outcome |
| AC-R20 — requirements are constraints | PASS | `packages/sci/src/registry.test.ts` — `SolverResolver` returns `incompatible` with a reason for unsupported temperature/species/activity capability; an empty registry returns `unavailable` |
| Result envelope has no bare scalar shortcut | PASS | `packages/sci/src/public-api.guarantees.ts` + `pnpm verify:guarantees` — `SolveResult.ph` is a compile error |
| Result statuses are exercisable | PASS | `packages/sci/src/adapter.test.ts` — `OK`, `MODEL_OUT_OF_DOMAIN`, `NOT_CONVERGED`, and `INVALID_INPUT` |
| Requirements are canonicalized at the DTO bridge | PASS | `packages/sci/src/request.test.ts` — `25 degC` becomes `298.15 K`; strict unknown keys are rejected |
| Applicability is machine-checkable | PASS | `ModelDescriptorSchema` requires `validity.activityCorrected`; schema contract test and committed `solve-result.schema.json` artifact |
| Source/data provenance remains distinct | PASS | `packages/sci/src/result.ts` re-exports schema-owned `Provenance`; no data provenance substitution is introduced |

## Runtime contract checks

- `SolverAdapter.solve` is `Promise<SolveResult>` for every adapter, including
  the synchronous-internally stub.
- `INVALID_INPUT` is returned before model-domain refusal when a typed request
  is malformed; no configured solver outcome is invoked.
- Exact registry lookup is keyed by the pair `(id, version)` and explicitly
  refuses fallback to another version.
- Requirements are checked against descriptor temperature, closed species set,
  solvent, phase, and activity-correction capability. A description string is
  not used as an applicability rule.
- Solver disagreement remains an investigation finding. No averaging or
  silent selection policy is implemented in M3.

## Reproduction

```text
pnpm typecheck
pnpm build
pnpm test
pnpm verify:guarantees
pnpm verify:schema-artifacts
pnpm depcruise
pnpm guards
pnpm verify:world
pnpm lint
uv run pytest
uv run python tools/check_acceptance_coverage.py
```

Local result for the implementation baseline: 14 test files / 168 tests,
65 Python tests, 71/71 acceptance criteria covered, and all listed commands
passed. The build regenerated and checked 9 committed JSON Schema artifacts.

## Known limitations carried forward

- The stub adapter is a contract fixture, not a scientific model. The v0
  `acidbase-monoprotic-davies` implementation and PHREEQC oracle validation are
  M4 scope.
- The resolver returns a selected compatible adapter/model for world creation
  to record. Persistence availability tiers, re-solve, and archive behavior
  remain M8 scope under ADR-0008.

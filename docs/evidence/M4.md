# M4 Evidence — Acid-Base Scientific Reality Core

**Status:** **S3 — Verified / Accepted**, owner acceptance recorded 2026-09-13
against implementation baseline `bb6a477d` and hosted CI `34747266204`

**Plan:** [`2026-09-12-m4-acid-base-engine.md`](../superpowers/plans/2026-09-12-m4-acid-base-engine.md)

**Approved design:** [`2026-09-12-m4-acid-base-engine-design.md`](../superpowers/specs/2026-09-12-m4-acid-base-engine-design.md)

**Cross-system remediation companion:** [`cross-system-contract-remediation.md`](cross-system-contract-remediation.md)

**Persisted schema migration companion:** [`m4-persisted-schema-v3-migration.md`](m4-persisted-schema-v3-migration.md)

**Evidence integrity research note:** [`m4-evidence-integrity.md`](../research/m4-evidence-integrity.md)

**PHREEQC offset investigation:** [`m4-phreeqc-offset-investigation.md`](../research/m4-phreeqc-offset-investigation.md)

**Validation baseline:** the canonical REF and PHREEQC oracle namespaces are
separate. The local oracle report is generated from the implementation baseline
recorded in its `validation` object; CI independently repeats the same required
command. The implementation baseline and hosted CI attestation are recorded
below.

`tools/check_acceptance_coverage.py` is a mapping/evidence-attachment checker:
it verifies that criteria are claimed by the plan and that the claiming
milestone names evidence. It does not establish semantic sufficiency. The
criterion-specific tests and guards below are the evidence for scientific
properties such as datum coverage and the complete v0 sweep.

The constants record, v0 scientific-input manifest, PHREEQC manifest,
hand-authored reference fixtures, independent Decimal derivation, and the
pinned PHREEQC process are exercised locally and by the oracle command below.
The complete matrix and bounded PHREEQC disposition have been reviewed and are
recorded as accepted in this transition. The scientific rows are individually
exercised. The model-identity prerequisites are closed:
NaOAc now joins
the common acid family, `waterActivity` is a frozen global solver parameter,
and scenario indicator constants are resolved, provenanced, hashed, migrated,
and copied into requests only from the frozen snapshot.

The companion packet records the pre-reference cross-system remediation and its
local verification. It does not alter the accepted REF/PHREEQC evidence or
promote the bounded PHREEQC comparison to model equivalence.

## Committed-baseline CI attestation

The final implementation baseline for this evidence packet is
`bb6a477d36a394ba5b18a671dafb4e0bc5055ee9` (`Close template literal molarity
bypass`). Hosted CI run `34747266204` / CI #90 completed with `success` for
that exact commit. The run covered the repository's type, build, test, guard,
browser, Python, PHREEQC, cross-engine, and acceptance-coverage gates.

This CI attestation is the implementation evidence used for the M4 S3 owner
acceptance recorded by this governance transition. It does not alter solver
behavior, scientific inputs, reference identities, or acceptance criteria.

## Acceptance matrix

| Criterion | Result | Evidence |
|---|---|---|
| AC-S1 | PASS locally | Canonical SPEC-0001 REF-1…REF-10 pass in `packages/sci/test/reference.test.ts` and `tools/oracle/tests/test_reference.py`; PHREEQC ORACLE-* fixtures are intentionally not used as REF evidence |
| AC-S2 | PASS locally | Every checked-in reference result satisfies the unquantized charge-residual bound in `packages/sci/test/reference.test.ts`; independent expected values are re-derived by the Python suite |
| AC-S3 | PASS locally | `apps/web/src/m4-acceptance.test.ts` creates a resolved four-component world, performs 100 accepted transfers, checks model-declared Na/Cl/acid-family totals with relative error ≤ `1e-13`, rejects negative inventory, and solves the resulting target inventory through the Scientific Adapter |
| AC-S4 | PASS locally | `packages/sci/src/registry.test.ts`, `apps/web/src/world-creation.test.ts`, and `packages/sci/src/acidbase/adapter.test.ts` cover requirements-level solvent/phase/species/component refusal plus adapter temperature, component, analytical-total, and converged-ionic-strength boundaries |
| AC-S5 | PASS locally | `ADVERSARIAL-HOAC-DILUTE.json` matches the independent exact solve and records the approximately `0.65` pH Henderson–Hasselbalch divergence in `packages/sci/test/reference.test.ts` |
| AC-S6 | PASS locally | [`M4-oracle-sweep-report.json`](M4-oracle-sweep-report.json) records all ten ORACLE-* TS↔PHREEQC comparisons, including pre-/at-/post-equivalence, with max absolute pH difference `0.0193938057606573` ≤ `0.02`; signed offsets and the `bounded-offset-no-equivalence-claim` disposition are explicit, and no equivalence is claimed |
| AC-S7 | PASS locally | `docs/research/constants-provenance.md`, `.json`, and `v0-scientific-inputs.json` record solver inputs, indicators, and every v0 concentration/density/molar-mass datum. The provenance checker rejects invented density pressure, checks source-faithful NaOH precision and the Perry 8th/Table 2-109 HOAc derivation, and distinguishes direct records from the explicit NaOAc model approximation. |
| AC-S8 | PASS locally | `packages/sci/src/projection.test.ts`, reference projection checks, type guarantees, and AST-based `pnpm verify:scientific-quantities` confine branded molarity symbols, generic dimension/schema/parser construction, and canonicalization of explicit molarity values to `ScientificProjection`; direct-import, namespace, dynamic-property, generic-dimension, generic-parser, direct-canonicalizer, template-canonicalizer, and explicit `molarity`/`mol/L`/`mmol/L` negative fixtures must fail. |
| AC-S9 | PASS locally | `Ph`/`TeachingHydrogenIonExponent` compile fixture and canonical REF-5/REF-6 projection references pass in the independent fixture suite |
| AC-S10 | PASS locally | `packages/sci/src/deterministic-math.test.ts` covers independent in-domain ULP vectors and explicit out-of-domain/non-finite refusal; `pnpm verify:scientific-math` passes |
| AC-S11 | PASS locally | `packages/sci/src/acidbase/solve.test.ts` machine-checks strictly increasing outer charge residuals for ordinary, high-concentration, and legal-side ionic-strength-boundary hydrogen sweeps |
| AC-S12 | PASS locally | ScientificState requires activity-based model-pH provenance and the scientific contract/docs distinguish it from the taught quantity; inspection copy remains M5 AC-V10 |
| AC-S13 | PASS locally | `packages/sci/src/acidbase/adapter.test.ts` exercises `0.15` and `0.30 mol/kg` HCl, asserts measured `I_m` matches each requested point, and requires both in-domain results to carry `withinProposedAccuracyEnvelope: false` |
| AC-S14 | PASS locally | `apps/web/src/m4-acceptance.test.ts` runs both eight-point families through `Scenario → WorldCreated → canonical WorldState → SolveRequest → SolverAdapter`, records every `I_m`, and compares the maximum to the separately frozen `v0-envelope-reference.json` (`0.09996461252716539 mol/kg`, strong-acid/strong-base, 0 equivalents) and the proposed `0.12` envelope. The reference pins the exact input-manifest SHA-256. |
| AC-S15 | PASS locally | `packages/schema/src/contracts.test.ts` rejects a scenario with missing density; density is required and no default is supplied |
| AC-S16 | PASS locally | `uv run pytest tools/oracle/tests/test_constants_provenance.py` checks source precision, machine-readable provenance, and logarithmic pKa→Ka uncertainty propagation |

AC-S13's scientific half is the `withinProposedAccuracyEnvelope` flag; its
visible qualification is deferred to M5 AC-V11.

## Deferred M5 presentation criteria

These criteria are intentionally not M4 S3 prerequisites. M4 supplies the
scientific model identity and accuracy-envelope flag; M5 owns the observable and
inspection presentation that makes those facts visible to a learner.

| Criterion | Result | Evidence owner |
|---|---|---|
| AC-V10 | DEFERRED TO M5 | M5 copy review and DOM assertion: model pH is never called true/thermodynamic pH, and the IUPAC notional convention plus activity model are named |
| AC-V11 | DEFERRED TO M5 | M5 deterministic fixture and DOM assertion: a false `withinProposedAccuracyEnvelope` flag is visibly qualified |

## Completed implementation slices

| Slice | Result | Reproducible evidence |
|---|---|---|
| PHREEQC/toolchain provenance | PASS locally | `uv run pytest tools/oracle/tests/test_manifest.py tools/oracle/tests/test_phreeqc_runner.py`; the report records the verified source, executable, and database identity |
| v0 catalog and fixed model identity | PASS | `pnpm exec vitest run packages/sci/src/acidbase/catalog.test.ts packages/sci/src/acidbase/model.test.ts` |
| Deterministic `detLog10`/`detExp10` boundary | PASS locally | `pnpm exec vitest run packages/sci/src/deterministic-math.test.ts`; `pnpm verify:scientific-math` |
| Native logarithm/exponential substitution guard | PASS locally | `pnpm verify:scientific-math` |
| Scientific molality/molarity boundary guard | PASS locally | `pnpm verify:scientific-quantities` and its illegal-core self-tests for branded symbols, generic dimension/schema construction, parsing, canonicalization, no-substitution template literals, and explicit molarity dimension literals |
| Davies activity and reduced species algebra | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/activity.test.ts packages/sci/src/acidbase/species.test.ts`; `pnpm verify:guarantees` |
| Nested bracketed reduced solve | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/solve.test.ts`; `pnpm typecheck` |
| Indicator activity equilibrium ratio | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/indicator.test.ts` |
| ScientificProjection boundary | PASS locally | `pnpm exec vitest run packages/sci/src/projection.test.ts`; `pnpm verify:guarantees` |
| Common acetate-family semantics | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/catalog.test.ts packages/sci/src/acidbase/solve.test.ts` |
| Frozen scenario indicator request bridge | PASS locally | `pnpm exec vitest run packages/sci/src/request.test.ts packages/world/src/state.test.ts` |
| v1→v2→v3 persisted migration, canonical temperature, and derived checksum rebuild | PASS locally | `pnpm exec vitest run packages/schema/src/contracts.test.ts packages/world/src/state.test.ts` |
| Explicit unit `waterActivity` convention and `Kw = a_H · a_OH` semantics | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/model.test.ts packages/sci/src/acidbase/solve.test.ts` |
| Analytical total-solute domain gate is checked before solving | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/adapter.test.ts` |
| Numerical bracket failure is tagged `NOT_CONVERGED`, not domain refusal | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/solve.test.ts` |
| Legal high-concentration strong-acid cases do not fail because root-bracketing evaluates Davies outside the inner domain | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/solve.test.ts packages/sci/src/acidbase/adapter.test.ts`; exact `0.500000 mol/kg` HCl/NaOH boundaries classify by the legal boundary evaluation |
| Numerical failure diagnostics carry a code/reason and no fabricated zero residual | PASS locally | `pnpm exec vitest run packages/schema/src/contracts.test.ts packages/sci/src/acidbase/solve.test.ts` |
| Solver-identity constants have machine-readable source precision metadata | PASS locally | `uv run pytest tools/oracle/tests/test_constants_provenance.py`; logarithmic pKa→Ka uncertainty is propagated rather than compared as ordinary significant digits |
| PHREEQC executable identity is pinned before oracle evidence | PASS locally | `uv run pytest tools/oracle/tests/test_phreeqc_runner.py` plus the required oracle run; CI-built runs require generated `toolchain.json`, while local overrides are explicitly unverified |
| Canonical REF-1…REF-10 validation | PASS locally | `pnpm exec vitest run packages/sci/test/reference.test.ts`; `uv run pytest tools/oracle/tests/test_reference.py`; Decimal derivation is separate from `packages/sci` and tamper-tested |
| ORACLE TS↔PHREEQC report completeness | PASS locally | `CHEMREALM_REQUIRE_PHREEQC=1 uv run python tools/oracle/reference/run_m4_validation.py --output docs/evidence/M4-oracle-sweep-report.json`; report has 10/10 ORACLE points, signed differences, and `pass: true` |
| Production acid-base adapter boundary | PASS locally | `pnpm exec vitest run packages/sci/src/acidbase/adapter.test.ts`; `pnpm typecheck`; `pnpm verify:guarantees` |

The deterministic math tests use independent high-precision reference literals,
ULP-distance checks, explicit refusal outside the measured domains, and a
subnormal/non-finite refusal matrix. `detExp10` is currently public only for the
validated Davies band `[-0.137, 0]`; widening that domain requires new reference
vectors and evidence.

## Task 5 ownership evidence

| Quantity | Owner | Input/output boundary |
|---|---|---|
| `m̂`, physical molality, activity, `γ`, ionic strength, model pH | Scientific Core | remains inside `ScientificState` |
| Indicator protonation ratio | Scientific Core | computed from `KaIn`, `a(H⁺)`, and `γ(In⁻)`; later observable input is only `{ indicatorId, protonationRatio }` |
| `c(H⁺)` | `ScientificProjection` | `H⁺` amount from `ScientificState` plus world `liquidVolume` via `molarityOf` |
| taught `−lg c(H⁺)` | `ScientificProjection` | deterministic `detLog10` of projected molarity; distinct `TeachingHydrogenIonExponent` |
| Colour, geometry, formatting | Observable/renderer layers | not implemented or imported by this M4 slice |

Task 5 tests also exercise the labelled monoprotic approximation boundary: the
ratio remains a continuous model calculation through the taught transition and
does not silently clamp at the high-pH edge. The SPEC's warning that a
monoprotic phenolphthalein approximation is not valid for reasoning about the
second transition above approximately pH 12 remains a caller/result-labeling
responsibility; no hidden pH threshold was added to the equilibrium function.

## Toolchain prerequisite

The PHREEQC release, extracted `phreeqc.dat` checksum, and CI-built executable
identity metadata are recorded in
[`tools/oracle/phreeqc/manifest.json`](../../tools/oracle/phreeqc/manifest.json).
The official USGS download page lists the pinned `3.8.6-17100` source archive
and batch distributions. `install_ci.sh` provides the checked-in Linux build
path and writes `toolchain.json`; `run_batch.py` verifies that metadata and the
database before invoking the CLI. The actual local run is recorded in
[`M4-oracle-sweep-report.json`](M4-oracle-sweep-report.json); CI runs the same
installation and validation command as a hard gate.

The cross-engine report uses `ORACLE-9` at `0.14 mol/kg` NaOH as the
representative post-equivalence point. This is a declared oracle fixture choice
within the `±0.02` pH comparison envelope. Canonical `REF-9` remains the
charge-conservation acceptance sweep; the two namespaces must not be merged.

## Next stop/go

The identity closure, high-domain bracket fix (including the exact public
boundary), diagnostic contract, precision-provenance record, executable
identity gate, production adapter slice, cross-system compatibility closure,
independent REF matrix, pinned PHREEQC cross-check, and the previously missing
AC-S3/AC-S4/AC-S7/AC-S8/AC-S11/AC-S13/AC-S14 fixtures are locally complete.
The v0 material inputs are sourced from `docs/research/v0-scientific-inputs.json`;
the independent AC-S14 output is frozen separately in
`docs/research/v0-envelope-reference.json`.
M4 S3 is accepted here because the PHREEQC offset has the explicit bounded
non-equivalence disposition, the complete matrix is evidenced, the committed
baseline CI attestation is recorded above, and the owner has accepted SPEC
revisions 13–20 plus ADR-0011 and ADR-0012. AC-V10 and AC-V11 are M5
presentation criteria and are intentionally not required for M4 S3.

## Candidate indicator multiform extension (SPEC revision 28)

The accepted M4 S3 result above remains unchanged. The multi-form indicator
extension is a separate candidate boundary documented in
[`2026-09-14-indicator-multiform-scientific-model.md`](../superpowers/specs/2026-09-14-indicator-multiform-scientific-model.md)
and its independent reference matrix.

Current status is **S2 candidate implementation / ordinary-aqueous scope
owner-accepted; M4 replacement and production rollout pending**:

- the ordinary aqueous phenolphthalein candidate names `H₂In`, `HIn⁻`, and
  `In²⁻` and requires distinct source-backed constants plus coupled balances;
- the strong-acid cation/orange case and strong-base-altered case remain
  explicit refusals, not unimplemented colours;
- Task 8 currently admits no quantitative indicator optical profile;
- the owner has accepted the ordinary-aqueous three-form scope and evidence
  contract on 2026-09-14, so implementation may proceed through the detailed
  Task 9 plan;
- the candidate implementation and independent MF reference matrix now pass
  locally at the committed handoff recorded in
  [`indicator-multiform.md`](indicator-multiform.md); the existing M4
  production adapter is not silently replaced and no M4 S3 replacement is
  claimed;
- the strong-acid cation/orange case remains documented but refusal-only: no
  species constants, solver branch, optical profile, or rendered orange output
  is authorized.

This candidate does not retroactively alter the accepted monoprotic M4 model,
its solver identity, its reference evidence, or its S3 status.
